pipeline {
    agent any

    environment {
        TODAY = "Monday" //local variable
    }

    stages {
        stage('Hello') {
            steps {
                echo "TODAY is : ${TODAY}"
                echo "Build Number : ${env.BUILD_NUMBER}" //Built-in variables
                echo "Branch Name : ${env.BRANCH_NAME}"
                echo "Branch Primary : ${env.BRANCH_IS_PRIMARY}"
                echo "Job Name : ${env.JOB_NAME}"
                
            }
        }
    }
}
